# 你在「人機協同寫作 pipeline」的 skill repo

⚠️ **這個 repo 是公開的**（`github.com/Legendream/AI-Writing-Skill`）。
commit 就是發布，且留在 git 歷史裡清不掉。

## 這個 repo 放什麼

**寫作方法資產，不是文章倉庫。** 內容是 skill 本體、模板、驗收清單、發布指引，
以及這些規則的依據文件。

先讀 `README.md`——那裡有**流程地圖**（七個階段各該看哪份文件）與資料夾地圖。

```
civic-tech-writing-pipeline/   ← 交付主體，要用就複製整個資料夾
docs/                          ← 規則的「為什麼」，不是使用說明
agents/                        ← 選配的 Claude Code 驗收代理
試跑紀錄/                       ← 不進版控的壓測工作區
```

## 三個 repo 的分工（重要，很容易搞混）

| 本機資料夾 | repo | 公開？ | 放什麼 |
|---|---|---|---|
| `AI Writing Pipeline/`（**你在這**）| `AI-Writing-Skill` | 公開 | 寫作方法資產 |
| `g0v civic tech guide/`（同階層）| `Taiwan-civic-tech-research` | 公開 | 交付給揪松團的問卷分析報告 |
| `g0v civic tech guide/05_文章產出（待撰寫）/` | 獨立的**私人** repo | **私人** | 文章草稿、成品、驗收紀錄 |

**這個 repo 的界線是刻意畫的**：依 g0v 案合約條款 3，本 skill 著作權歸乙方（Claire），
以 CC BY 4.0 授權**交付驗收之版本**（以 git tag 為準），乙方保留持續開發與另行運用的權利。
所以它不能跟客戶案的交付物混在一起。

**文章不要放進這個 repo。** 例外：通過驗收的成品，可挑一兩篇進 `examples/` 當範例凍結副本
（正本仍在文章 repo）——**成品當範例可以，草稿當倉庫不行**。

## 這裡的東西怎麼演化

規則不是設計出來的，是撞出來的：

```
實際寫一篇 → 出錯或被挑錯 → 問「為什麼這類錯漏得過去」
           → 寫成規則提案（docs/）→ 改 SKILL.md → 下一篇驗證
```

判斷標準：**只有那一篇會犯的錯，修完就結束；換一篇還會再犯的，寫進 `docs/`。**

## 常見陷阱

- **不要相信 AI 對自己工作的自評**，包括 SKILL.md 內建的強制查核關卡。
  那份自評的用途是「讓你知道該查什麼」，不是「證明已經查過了」。
  真正的關卡是交付前那雙沒看過產出過程的眼睛（`checklists/`）
- `試跑紀錄/` 底下有**不可公開的委託稿範文**。改 `.gitignore` 前必先跑
  `git check-ignore -v <路徑>` 確認仍被擋住
- `docs/` 裡的實測結果會過期（平台政策一直在變），文件內附自行重測的方法，
  **要重測不要照抄**
