# ============================================================
# 這個 repo 是「公開」的（github.com/Legendream/AI-Writing-Skill）。
# commit 就是發布，而且會留在 git 歷史裡，事後刪除也清不乾淨。寧可先擋。
# ============================================================

# ---------- 一、未經查核的試跑工作區 ----------

# 每次用真實題目壓測 skill 的工作資料夾：素材原檔、初稿、交接筆記。
# 這些是「查核未結案」的中間產物，不該公開；壓測結論要留下時，
# 寫成 docs/ 底下的規則提案，不要把工作區整包推上來。
#
# ⚠️ 資料夾裡可能放著他人著作的原文備份（來源網頁存檔）與**不可公開的委託稿範文**，
#    改動這幾行前務必先跑 `git check-ignore -v <路徑>` 確認仍被擋住。
試跑紀錄/
試跑-follow-the-money/
試跑-資料驅動對策手法/

# ---------- 二、個人風格檔的範文本體 ----------

# styles/*.md 只描述文風特徵，可公開；但範文原文屬作者個人作品，
# 其中智慧國家委託稿兩篇受委託方限制**永遠不得公開**（檔名與篇名皆不可出現）。
範文/
**/範文/

# ---------- 三、系統與暫存 ----------

.DS_Store
.claude/
