# 待辦總覽

**這份檔案只放「還沒做的事」。**

## 兩條維護紀律

1. **做完就刪掉，不是劃掉。** 每件事的來龍去脈已經寫在它自己的驗收文件裡
   （`docs/站①驗收條件_*.md`），完成的項目留在這裡沒有資訊價值，只有噪音。
2. **如果這份檔案變長的速度比變短快**，代表在累積問題而不是解決問題。
   那個訊號比檔案大小本身有意義。

（`docs/` 不隨 skill 的 zip 交付，所以這份檔案不會佔到使用者的 context。）

最後更新：2026-09-02

---

## 一、把 FAQ 上網站

草稿在 `docs/FAQ草案_跨工具與使用經驗.md`，**尚未 HTML 化**。要先定驗收條件。

⚠️ **同時要做**：`index.html` 與 `README.md` 的「開始前你要準備什麼」那張表，
現在寫著「獨立驗收沒過、66 條有 19 條未通過」，用的是 FAQ 已經拿掉的內部細節，
而它面對的也是還沒安裝的讀者。**兩邊語氣要一次對齊。**

## 二、建 `.github/ISSUE_TEMPLATE/`

目前不存在。三題範本與三類回饋門檻的規劃已完整寫在
`docs/人機分工與外部回饋.md`，直接落成檔案即可。該文件自己也說 README 應寫明
「文風請自帶風格檔或 fork；回報 issue 請限於正確性問題與流程摩擦」。

**不要把 issue 寫進網站 FAQ**：開 issue 需要 GitHub 帳號，跟這個網站
「服務不想碰 GitHub 的人」的初衷衝突。

## 三、獨立驗收累積的既有問題

以下由 `acceptance-reviewer` 扮演陌生讀者時發現，判定為既有問題而未併入當時的 PR。

**A 批・網站與 README 的落差**

- `index.html` 缺 README 的「**①④⑥ 不能略**」警告。README 說那三站是
  「這套東西唯一的賣點，略掉就跟直接叫 AI 寫一篇沒有差別」，而網站是對外入口，
  一句都沒有
- `index.html` 說「每一站做完才會進下一站」，README 說 ②③⑤ 可收合成一輪
- 兩種文體的定義在兩個檔講法不同，可能害讀者選錯
- 五份交付物沒有一份對應得到「在哪一站產出」

**B 批・宣稱與實情**

- 「只保證在 Claude 環境下運作」與整頁力推 Antigravity 衝突
- 工具表把 Claude Code 與 Antigravity 併列，但只有後者測過
- ChatGPT／Gemini 那列「沒有會自動觸發的 skill 安裝機制」是**未查證的機制斷言**，
  與同列「我們沒有實測過」自相矛盾
- 「省下大量排版」對不上八站裡的任何一站，也沒有對應產出
- 路徑二只講 Antigravity 跑完一篇，未提同一次實測 19/66 未通過

**C 批・術語與缺口**

- 「三個硬門檻」是哪三個、「建議模型：最強推理模型」是什麼，全頁未說明
- 「可信程度／查證程度／把握程度」三種叫法指同一件事
- **`agents/article-reviewer.md` 沒有任何安裝說明**（放到哪、怎麼叫），
  而它正是「最不能省的第 ⑥ 站」要用的東西
- 走路徑二的人，**素材該放哪**，兩份文件都沒交代
- `README.md` 的「`SKILL.md` 不在 repo 根目錄」與另外三處「根目錄有一份」字面相反
- 「哪幾站可以省」表格首欄無表頭

**D 批・技術雜項**

- `.placeholder` 是死 CSS（定義了，零處使用）
- HTML 註解含內部 TODO，且有一個簡體字「换」
- `og:image`／`og:url`／canonical 缺（註解自稱是分享用）
- 下載 zip 按鈕是全頁唯一沒有 `target="_blank"` 的外部連結

## 四、Skill 本體

- **`SKILL.md` 瘦身。** 2026-09-02 三次成功觸發，**三次都出現「讀取被截斷的中段」**。
  32,952 字元、20,669 個中文字；官方對 Level 2 的建議是 5k tokens
- **`description` 裡的「知識庫」**（兩處）。**不影響觸發**——驗證有效的是那五個
  主題詞，這兩處一處是用途描述、一處是沒人會講的觸發句。但它**暗示了一個不存在
  的義務**：使用者的文章不一定要放到那裡。正解不是換名字，是拿掉去處：

  > 舊：用於產製公民科技知識庫文章
  > 新：用於把公民科技的實作經驗，寫成別人能照著走的文章

  觸發句可改成真名「為台灣公民科技資料庫貢獻內容」。**跟瘦身一起做**，
  瘦身本來就要動整個檔、本來就要重測，順手改成本為零
- **`styles/default.md`**：把 `claire.md` 裡通用的條目抽出來當自動套用的預設，
  `claire.md` 留作填寫實例。公版不該預設套用作者本人的聲音
- **`§4-1` 一手／二手判準判不穩**：2026-09-02 四次觀察跑出兩種結論
  （★★★★☆ 兩次、★★★★★ 兩次）。判準需要寫得更可判定

## 五、還沒測的三件

- **網路出口對事實查核的影響**：`claude.ai/settings/capabilities` 有個
  「Allow network egress」開關。開與關各跑一次「請實際打開這個連結核對」，看有沒有差
- **獨立驗收的替代做法**：開新對話只貼驗收清單與成品，看驗不驗得動
- **「並安裝到我的 skills 資料夾」**這句在 Claude Code 是否真的有效
  （網站上已標明未實測）

**改 `description` 之後必跑的觸發實測**，判準是回覆最後一行出現「下一站：」：

1. 我想寫一篇公民科技相關的文章
2. 我想寫一篇介紹某個公民科技工具的文章
3. 我想寫一篇公民科技的方法論文章〔回歸〕
4. 我想整理某個領域的公民科技資源清單〔回歸〕

## 六、大工程

- **搬離 GitHub Pages ＋ 自建留言區。** 方向已定：Cloudflare Pages + D1 +
  Turnstile + Zero Trust，額外費用 $0。留言區定位是**配角**，不放首頁大版位；
  訪客免註冊、內容公開可見
- **整合網站**：問卷報告 → 這套 pipeline → 引用 civictech.tw，
  呈現「從問題發現到嘗試解法」。緣由那段已經把敘事線的第一塊接上了
- **HTTPS 憑證**：2026-09-02 查仍未簽發。搬站之後這條自動消失
