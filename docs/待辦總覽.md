# 待辦總覽

**這份檔案只放「還沒做的事」。**

## 兩條維護紀律

1. **做完就刪掉，不是劃掉。** 每件事的來龍去脈已經寫在它自己的驗收文件裡
   （`docs/站①驗收條件_*.md`），完成的項目留在這裡沒有資訊價值，只有噪音。
2. **如果這份檔案變長的速度比變短快**，代表在累積問題而不是解決問題。
   那個訊號比檔案大小本身有意義。

（`docs/` 不隨 skill 的 zip 交付，所以這份檔案不會佔到使用者的 context。）

最後更新：2026-09-05

---

## 一、建 `.github/ISSUE_TEMPLATE/`

目前不存在。三題範本與三類回饋門檻的規劃已完整寫在
`docs/人機分工與外部回饋.md`，直接落成檔案即可。該文件自己也說 README 應寫明
「文風請自帶風格檔或 fork；回報 issue 請限於正確性問題與流程摩擦」。

**不要把 issue 寫進網站 FAQ**：開 issue 需要 GitHub 帳號，跟這個網站
「服務不想碰 GitHub 的人」的初衷衝突。

## 二、獨立驗收累積的既有問題

以下是獨立驗收或事後比對時發現、判定為既有問題而未併入當時 PR 的項目。

**`README.md` 中文句中換行造成的多餘空格**

- Markdown 的軟斷行在 GitHub 渲染時會變成一個空格，中文之間本來不該有空格，所以
  「⋯每一塊站得住，⏎ 同時讓讀者一眼看出⋯」（`README.md:11-12`）會渲染成句子中間有
  肉眼可見的空隙。目前 **60 處**（2026-09-04 量測），其中 2 處是刻意空格
  （第 43 行「一篇文章 ＋ 一份」、第 109 行「叫 `素材` 的」），實際要修 58 處
- **改法**：只動斷行位置，一個字都不改；中文句子在 source 裡不斷行，區塊邊界的斷行
  不用動。代價是行會變長（先前在網站的單頁做過同樣的修，最長約 300 字元，那是刻意的）
- **重測方法**（數字會隨改動變，不要照抄）：掃 `README.md` 每一對相鄰行，跳過程式碼
  圍欄內的行，以及**下一行**是表格行、標題行、清單或引用起始行的配對；若前一行結尾與
  下一行開頭都是中日韓字元或全形標點，就算一處。2026-09-04 這樣量到 60 處
- ⚠️ **這個量法只適用 Markdown，不要拿去量 HTML。** HTML 檔量出來的多半落在 CSS 註解
  或區塊邊界，不會渲染成空格，是假的。HTML 要量請量渲染後的文字（瀏覽器 `innerText`），
  不要量 source

## 三、還沒測與待查證的

- **建 Custom GPT 需不需要付費方案？**〔待查證，不是實測〕
  網站對 claude.ai 的 Pro 門檻標得很清楚（附官方文件連結與查證日期），
  但改推 Custom GPT／Gem 之後，同類門檻一個字都沒提。
  2026-09-03 獨立驗收查 OpenAI 官方說明頁時全數吃到 403 或驗證牆，
  **查不到就沒有寫**。查得到之後：若確有方案門檻，比照 claude.ai 那條加註並附來源；
  若沒有，也記一筆免得下次再查一遍
- **`default.md` 讓位還沒測**〔2026-09-05 PR #88 留下〕
  使用者自帶風格檔時，`styles/default.md` 應該整份讓位、不疊加。四句觸發實測驗的是
  「都沒給時會不會套預設」，這一條沒測到（額度用盡）。測法：開新對話貼一份三行的
  自製風格檔（例如標題習慣、語氣、結尾各一行），看它套使用者那份還是回頭套預設。
  驗收條件與已完成的部分見 `docs/站①驗收條件_風格檔去個人化.md` E 組

- **網路出口對事實查核的影響**：`claude.ai/settings/capabilities` 有個
  「Allow network egress」開關。開與關各跑一次「請實際打開這個連結核對」，看有沒有差
- **獨立驗收的替代做法**：開新對話只貼驗收清單與成品，看驗不驗得動
- **「並安裝到我的 skills 資料夾」**這句在 Claude Code 是否真的有效
  （網站上已標明未實測）

## 四、網站相關的待辦已經搬走

2026-09-05 起，網站不住在這個 repo 了。原始碼與待辦都在
[Legendream/g0v.civictech-pipeline](https://github.com/Legendream/g0v.civictech-pipeline)，
網址是 `civictech.claire-cheng.com`。

**搬過去的**：分享圖（`og:image`）、自建留言區、整合網站敘事
（問卷報告 → 這套 pipeline → 引用 civictech.tw），以及「常見問題第一題結尾那句
歡迎留言告訴我們結果，要等留言區到位才成立」這筆債。

**已經消失的**：HTTPS 憑證。舊站 `write.claire-cheng.com` 於 2026-09-05 關閉，
DNS 記錄與 GitHub Pages 設定都刪了，這條不再存在。

這個 repo 從此只放寫作方法資產，跟 `CLAUDE.md` 寫的界線一致。
